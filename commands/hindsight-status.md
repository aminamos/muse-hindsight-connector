---
description: Show Hindsight memory status for the current project
argument-hint: "[bank-id]"
---

Report Hindsight memory status for this project$ARGUMENTS. Use the `hindsight_status`
MCP tool (server `hindsight`); if a bank id argument was given, recall one probe
query against it with `hindsight_recall` and include the hit count. Keep the
report to server URL, project, bank id, fact count (or not-created-yet), and one
line on what the hooks will do this session.
